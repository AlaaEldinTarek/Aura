<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Permissions for connectivity monitoring -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <!-- Permissions for prayer times -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- Do Not Disturb Permission for Silent Mode -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY"
        android:minSdkVersion="23" />

    <!-- Focus Mode Permissions -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.REORDER_TASKS" />

    <!-- Storage permissions for downloading adhan files -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="Aura - هالة"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Adhan Full-Screen Activity - Shows over lock screen when prayer time arrives -->
        <activity
            android:name=".AdhanFullScreenActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true">
        </activity>

        <!-- Focus Mode Activity - Full screen lock for task focus -->
        <activity
            android:name=".FocusModeActivity"
            android:exported="false"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:launchMode="singleTop"
            android:taskAffinity="com.aura.hala.focus"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true">
        </activity>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Prayer Time Alarm Receiver -->
        <receiver
            android:name=".PrayerAlarmReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.aura.hala.PRAYER_TIME_ALARM"/>
            </intent-filter>
        </receiver>

        <!-- Stop Adhan Receiver -->
        <receiver
            android:name=".StopAdhanReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.aura.hala.STOP_ADHAN"/>
            </intent-filter>
        </receiver>

        <!-- Prayer Boot Receiver -->
        <receiver
            android:name=".PrayerBootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="android.intent.action.ACTION_BOOT_COMPLETED"/>
                <action android:name="android.intent.action.REBOOT"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            </intent-filter>
        </receiver>

        <!-- Prayer Reschedule Service -->
        <service
            android:name=".PrayerRescheduleService"
            android:enabled="true"
            android:exported="false"
            android:stopWithTask="false"/>

        <!-- Prayer Foreground Service - Keeps app alive for prayer alerts -->
        <service
            android:name=".PrayerForegroundService"
            android:enabled="true"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="dataSync"/>

        <!-- Silent Mode Receiver - Handles silent mode toggle from Adhan notification -->
        <receiver
            android:name=".ToggleSilentModeReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.aura.hala.TOGGLE_SILENT"/>
            </intent-filter>
        </receiver>

        <!-- Silent Off Receiver - Auto restores ringer after timer -->
        <receiver
            android:name=".SilentOffReceiver"
            android:enabled="true"
            android:exported="false"/>

        <!-- Focus Mode Receiver - Triggers focus mode at task time -->
        <receiver
            android:name=".FocusModeReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.aura.hala.FOCUS_MODE_ALARM"/>
            </intent-filter>
        </receiver>

        <!-- Focus Mode Service - Foreground service that prevents focus mode dismissal -->
        <service
            android:name=".FocusModeService"
            android:enabled="true"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="dataSync"/>

        <!-- Daily Content Widget (Ayah / Hadith) -->
        <receiver
            android:name=".DailyContentWidget"
            android:label="@string/daily_content_widget_label"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/daily_content_widget_info"/>
        </receiver>

        <!-- Next Prayer Widget -->
        <receiver
            android:name=".NextPrayerWidget"
            android:label="@string/next_prayer_widget_label"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
                <action android:name="com.aura.hala.ACTION_SWITCH_VIEW"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/next_prayer_widget_info"/>
        </receiver>

        <!-- Combined Prayer Widget (Next Prayer + Timeline with ViewFlipper) -->
        <receiver
            android:name=".AllPrayersWidget"
            android:label="@string/all_prayers_widget_label"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
                <action android:name="com.aura.hala.ACTION_SWITCH_TAB"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/combined_prayer_widget_info"/>
        </receiver>

        <!-- Widget Broadcast Receivers -->
        <receiver
            android:name=".NextPrayerWidgetReceiver"
            android:enabled="true"
            android:exported="false"/>

        <receiver
            android:name=".AllPrayersWidgetReceiver"
            android:enabled="true"
            android:exported="false"/>

        <receiver
            android:name=".DailyContentWidgetReceiver"
            android:enabled="true"
            android:exported="false"/>

        <!-- Tasks Widget -->
        <receiver
            android:name=".TasksWidget"
            android:label="@string/tasks_widget_label"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/tasks_widget_info"/>
        </receiver>

        <receiver
            android:name=".TasksWidgetReceiver"
            android:enabled="true"
            android:exported="false"/>

        <!-- Next Task Widget (4x1) -->
        <receiver
            android:name=".NextTaskWidget"
            android:label="@string/next_task_widget_label"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/next_task_widget_info"/>
        </receiver>

        <receiver
            android:name=".NextTaskWidgetReceiver"
            android:enabled="true"
            android:exported="false"/>

        <!-- AuraAccessibilityService removed — screen pinning dialog handled manually -->
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
