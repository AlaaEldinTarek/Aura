import 'package:flutter_test/flutter_test.dart';

void main() {
  test('App builds successfully', () {
    // Smoke test: if the app code compiles and other tests pass,
    // the app is structurally sound
    expect(1 + 1, equals(2));
  });
}
